node_modules/
.next/
.project_id
.github_repo
.env
.env.*
.vercel/
.nitro/
dist/


# Incremental tsc cache. Generated by every `npm run typecheck`, so tracking it
# means the tree is dirty after any build — which is how real changes get lost
# in the noise of a stop-hook warning.
tsconfig.tsbuildinfo

# Throwaway verification harnesses and their screenshots. These drive the real
# build in a real browser and are worth keeping ON DISK for the session; none
# of it is the game.
scratchpad/
